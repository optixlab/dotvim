# test python (sample from offlineimap)

class ExitNotifyThread(Thread):

def run(self):
  test = function():
    if(this):
      x = 7
    else:
      y = 3
      
foreach name in names:
  x = name
  print name

"""This class is designed to alert a "monitor" to the fact that a thread has exited and to provide for the ability for it to find out why."""

"""This class is designed to alert a "monitor" to the fact that a thread has exited and to provide for the ability for it to find out why."""






